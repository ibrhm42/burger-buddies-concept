# Safe default for local development and sales demonstrations.
# When true, checkout opens an in-app preview and must not contact Burger Buddies automatically.
NEXT_PUBLIC_DEMO_MODE=true

# Configure only a deliberate test destination when external WhatsApp testing is required.
# Use international digits without +, spaces, or hyphens.
NEXT_PUBLIC_WHATSAPP_NUMBER=923XXXXXXXXX
